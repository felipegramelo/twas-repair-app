<analysis>**original_problem_statement**: 
The user wants to unify two separate applications (a Timesheet Tracker and a Service/Daily Report app) into a single TWAS REPAIR app. The application requires role-based authentication (Admin/Supervisor), full CRUD functionalities for Timesheets and Reports, and an advanced A4 PDF generation system with custom headers, footers, watermarks, dynamic tables of contents, and photo/PDF attachments.

PRODUCT REQUIREMENTS:
- Role-based authentication (Admin, Supervisor).
- Full CRUD for Timesheet, Service Orders, and Service/Daily Report entities.
- Advanced PDF generation engine (ReportLab/PyMuPDF) managing complex page breaks, custom borders, multi-level TOC, embedded images, and signatures.
- Boletim de Medição (Measurement Bulletin) feature for billing calculation based on timesheet functions and dynamic client pricing.
- Granular administrative permissions to hide/show specific financial or structural panels.

**User's preferred language**: Portuguese (pt-BR). INSTRUCT next agent to respond in the user's preferred language only.

**what currently exists?** 
A full-stack application with a React Native (Expo Web) frontend, a FastAPI backend, and MongoDB. The core functionalities for Reports, Timesheets, and Service Orders are complete. The PDF generation is sophisticated and stable. Recent additions include an Arquivo por O.S. (consolidated document view per Service Order) and a Boletim de Medição (BM) system that automatically calculates service costs based on timesheets and generates landscape billing PDFs. Granular admin controls are implemented.

**Last working item**:
    - Last item agent was working: Modifying the Boletim de Medição (BM) calculation flow. The user requested two things: 1) The ability to select *specific* timesheets (via checkboxes) belonging to an OS to include in the BM calculation, rather than calculating all of them automatically. 2) Adding calendar date pickers for selecting the Data Início and Data Fim (Start and End dates) of the BM period.
    - Status: IN PROGRESS (Just started analyzing files before context limit).
    - Agent Testing Done: N
    - Which testing method agent to use? both (Frontend for the calendar and timesheet selection UI, Backend for the modified  endpoint payload).
    - User Testing Done: N

**All Pending/In progress Issue list**:
  Issue 1: BM Timesheet Selection & Calendar Pickers (P0)
     - Attempted fixes: The agent just checked  and the  endpoint in  to prepare for the changes. No code modifications were made yet.
     - Next debug checklist: 
       - : Modify  to optionally accept a list of  (or change to a  endpoint to accept payload), and adjust the date parsing. Also, create an endpoint to fetch available timesheets for a specific OS.
       - : Add UI to fetch and list available timesheets for the selected OS, allow the user to check/uncheck them, and implement Calendar components (e.g., HTML date inputs or React Native date pickers) for the period selection.
     - Why fix this issue and what will be achieved with the fix? Gives admins precise control over which timesheets are billed in a specific BM, avoiding duplicate billing if multiple BMs are generated for the same OS over different periods.
     - Status: IN PROGRESS
     - Is recurring issue? N
     - Should Test frontend/backend/both after fix? both
     - Blocked on other issue: None

**In progress Task List**:
  Task 1: Allow timesheet selection & add calendar for BM calculation (P0)
     - Where to resume: Modify the  endpoint logic in  and the calculation UI modal in .
     - What will be achieved with this? Precise measurement calculations.
     - Status: IN PROGRESS
     - Should Test frontend/backend/both after fix? both
     - Blocked on something: None

**Upcoming and Future Tasks**:
  Upcoming Tasks:
  - Add  (06-18 / 07-19) in the Service Orders (OS) UI (P1).
  - Refactor  (P1): The file is now approaching 3000 lines. It urgently needs to be modularized (e.g., , , ).
  
  Future Tasks:
  - Refactor  into smaller, reusable components (P2).
  - Offline Mode (P2): Implement  and a synchronization queue for offline functionality.
  - EAS Build (P2): Prepare  and  for App Store and Play Store deployment.

**Completed work in this session**:
- Tested and verified 10 pending PDF layout changes from the previous fork (signatures, borders, alignment).
- Implemented **Arquivo por O.S.** for admins to view consolidated Timesheets and Reports under a specific Service Order. Cleaned up the Admin dashboard.
- Implemented **Boletim de Medição (BM)** system:
  - Added Client Price Tables (CRUD).
  - Automated calculation mapping timesheet functions to daily rates.
  - Implemented automatic +20% calculation for night shift functions.
  - Built a new landscape PDF generation system specifically for BMs with the TWAS logo and custom layout.
- Added **Granular Admin Access Controls**: Added  and  flags. Added UI toggles in the Admins page to grant/revoke these permissions dynamically.
- Implemented a visual UI toast notification replacing  for PDF success messages.

**Earlier issues found/mentioned but not fixed**:
  - None active. The app performs well, but the user noticed minor slowness when generating PDFs. The agent noted this is due to preview environment constraints and will be faster in production, but no explicit optimization fix was applied yet.

**Known issue recurrence from previous fork**:
  - Issue recurrence in previous fork: **Aggressive Expo Metro Caching**.
  - Recurrence count: 3+
  - Status: IN PROGRESS (Agent frequently had to use  and  bash commands to force the UI to update during testing).
  - Note: Next agent must proactively restart Expo using bash if UI changes do not appear in screenshots.

**Code Architecture**:


**Key Technical Concepts**:
- **Frontend**: React Native for Web (Expo Router), TypeScript.
- **Backend**: FastAPI, MongoDB ().
- **PDF Generation**: ReportLab (layout/drawing) + PyMuPDF ( - for post-processing and page numbering).
- **Authentication**: JWT-based, stored in AsyncStorage, passed via HTTP Bearer and query strings (for mobile PDF handling).

**key DB schema**:
  - : { , , , ,  (bool),  (bool) }
  - : { , , , , , , ... }
  - : { , , ,  (employee, function, times), ... }
  - : { , , : { :  } }
  -  (Boletim de Medição): { , , , , , ... }

**changes in tech stack**:
  - None.

**All files of reference**:
  - : Contains endpoints for BM () and PDF logic.
  - : BM dashboard and calculation modal.
  - : Admin permission management.
  - : UI for report editing (recently updated with toast).

**Areas that need refactoring**:
  - : P1 Priority. Needs split into routes, models, and PDF utils.
  - : Very large component, needs splitting.

**key api endpoints**:
  - : Evaluates timesheets for an OS and calculates the billing amount. (CURRENTLY BEING MODIFIED).
  - : Generates the landscape BM PDF.
  - : Fetches consolidated documents by OS.

**Critical Info for New Agent**:
  - The immediate task is to update the BM calculation flow. You need an endpoint to list timesheets for an OS so the user can select them via checkboxes in . Then, pass those selected IDs and calendar dates to the backend to calculate the total.
  - The night shift +20% calculation rule is already implemented in the backend; you do not need to build it again, just maintain it when altering the  endpoint.
  - Restart the Expo server manually via bash (expo: ERROR (not running)
expo: started or stopping/starting via bash script) if your frontend changes aren't rendering.

**documents and test reports created in this job**:
  - 
  - 
  - 
  - 
  - 

**Last 10 User Messages and any pending HUMAN messages**:
1. **(PENDING)** User requests modifications to the BM: Select specific timesheets for an OS to calculate, and use calendar pickers for the start and end dates.
2. User says pode (yes) to proceed with the BM calculation changes.
3. User notices Encanador is missing from timesheet but shouldn't be in BM. Requests +20% automatic calculation for night shift functions and adding the TWAS logo to the BM PDF header.
4. User complains the PDF success message isn't showing and PDFs are slow to open.
5. User asks for the preview link.
6. User requests granular permissions for Boletim de Medição and Arquivo por O.S. per admin, and requests the BM PDF to be in landscape with the TWAS logo, similar to the service report border.
7. User requests removing Timesheets, Service Reports, and Daily Reports cards from the admin dashboard since Arquivo por OS handles this, and reports the back button in Arquivo por OS is broken.
8. User asks to create a Boletim de Medição card in the Admin dashboard, sharing a PDF model and rules (different pricing per client/day/night).
9. User asks if there is a storage limit for the app (Object Storage).
10. User asks for the preview link.

**Project Health Check**:
- **Working**: Backend, PDF generation, OS Archive, BM calculation.
- **In Progress/Needs Work**: The new timesheet selector and calendar picker for BM calculation.

**3rd Party Integrations**:
- Emergent Object Storage — uses Emergent LLM Key.

**Testing status**:
  - Testing agent used after significant changes: YES (Iterations 19-22)
  - Troubleshoot agent used after agent stuck in loop: NO
  - Test files created:  to .
  - Known regressions: None active.

**Credentials to test flow**:
- Admin: admin@twasrepair.com / admin123
- Supervisor: supervisor@twasrepair.com / super123

**What agent forgot to execute**:
The agent stopped exactly after agreeing to the user's request to add timesheet selection and calendar date pickers for the BM calculation. No actual code modifications were started for this specific feature yet.</analysis>
